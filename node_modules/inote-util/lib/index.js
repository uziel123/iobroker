// Generated by CoffeeScript 1.9.3
(function() {
  var HOMEDIR, LIB, LIB_COV, LIB_DIR, exported, file, fs, i, k, len, path, sources, v;

  fs = require('fs');

  path = require('path');

  HOMEDIR = path.join(__dirname, '..');

  LIB_COV = path.join(HOMEDIR, 'lib-cov');

  LIB = path.join(HOMEDIR, 'lib');

  LIB_DIR = fs.existsSync(LIB_COV) ? LIB_COV : LIB;

  sources = ['util', 'io-util', 'object-util', 'stopwatch', 'config'];

  for (i = 0, len = sources.length; i < len; i++) {
    file = sources[i];
    exported = require(path.join(LIB_DIR, file));
    for (k in exported) {
      v = exported[k];
      exports[k] = v;
    }
  }

}).call(this);
